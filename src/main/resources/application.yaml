spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/store_api?createDatabaseIfNotExist=true
    username: root
    password: "qwe123QWE!@#"

# Show exception message in 500 responses (helps debug)
server:
  error:
    include-message: always
    include-stacktrace: always
    include-exception: true